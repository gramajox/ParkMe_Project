<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.xgramajo.parkme_ids_2018">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Permisos para la localización -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" /> <!-- Key de Maps -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!-- "@integer/google_play_services_version" -->
        <activity
            android:name=".home.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity
            android:name=".parking.ParkingActivity"
            android:label="@string/title_activity_parking"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login.PasswordActivity" />
        <activity android:name=".PaymentActivity" />
        <activity
            android:name=".PatentActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MonitorActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".EnableActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <!--
        ACTIVITY MERCADO PAGO
        <activity
            android:name=".mercadopago.CheckoutExampleActivity"
            android:label="@string/title_activity_checkout_example"/>
        <activity
            android:name=".mercadopago.JsonSetupActivity"
            android:label="@string/title_activity_checkout_example"/>

        <activity
            android:name=".mercadopago.SelectCheckoutActivity"
            android:label="@string/title_activity_checkout_example"/>
        -->

        <activity android:name=".parking.ChronometerActivity"></activity>
    </application>

</manifest>